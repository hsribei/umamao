<%= t('notifier.global.hi', :user => h(@user.first_name)) %>

<%= t('notifier.invitation_converted.message_text',
    :user => h(@invited_user.name),
    :profile_link => @invited_user_link,
    :first_name => h(@invited_user.first_name)) %>

<%= t('notifier.global.signature') %>
