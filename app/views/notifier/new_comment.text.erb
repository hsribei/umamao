<% question = link_to h(@question.title), url_for(:host => @group.domain,
                                                  :controller => "questions",
                                                  :action => "show",
                                                  :id => @question.slug) %>
<%= t('notifier.global.hi', :user => h(@user.first_name)) %>

<%= t('.author', :user => h(@comment.user.name), :question => question) %>

"<%= h(@comment.body) %>"

<%= simple_format(t('notifier.global.signature')) %>
