<%= t('notifier.global.hi', :user => h(@user.first_name)) %>

<%= t('.text_message', :origin => h(@origin.first_name),
      :entry => h(@entry.title)) %>

<%= url_for(:host => @domain, :controller => 'topics',
            :action => "show", :id => @entry.id) %>

<%= t('notifier.global.signature') %>
