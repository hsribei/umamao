= simple_format(t('notifier.global.hi', :user => h(@user.first_name)))

= simple_format(t('notifier.survey.message',
                  :application_name => AppConfig.application_name,
                  :one_week_survey_url => AppConfig.one_week_survey_url))

= simple_format(t('notifier.global.founder_signature',
                  :application_name => AppConfig.application_name))
